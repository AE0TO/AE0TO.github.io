/* ============================================================
   AE0TO TACTICAL OPERATIONS CENTER — FULL CSS
   ============================================================ */

:root {
  --bg-main: #020308;
  --bg-panel: #070b14;
  --bg-panel-alt: #0c101e;

  --accent-red: #ff4b6e;
  --accent-amber: #ffb347;
  --accent-cyan: #3cf2ff;
  --accent-blue: #4aa8ff;
  --accent-violet: #c77dff;
  --accent-green: #4cffb3;

  --text-main: #e6f1ff;
  --text-muted: #7f8ca8;

  --radius-lg: 18px;
  --radius-md: 12px;

  --shadow-hard: 0 0 50px rgba(0, 0, 0, 0.95);

  --font-sans: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-mono: "JetBrains Mono", "Fira Code", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Courier New", monospace;
}

/* RESET */

*,
*::before,
*::after {
  box-sizing: border-box;
}

body {
  margin: 0;
  padding: 0;
  font-family: var(--font-sans);
  background: radial-gradient(circle at top, #151b2f 0, #05070b 55%, #000000 100%);
  color: var(--text-main);
}

/* WIDTH SYSTEM */

.console-section {
  padding: 3.5rem clamp(1.5rem, 4vw, 4rem);
  margin: 0 auto;
}

.console-width-adaptive {
  max-width: 1400px;
}

.console-width-medium {
  max-width: 1200px;
}

.console-width-narrow {
  max-width: 800px;
}

/* Adaptive widescreen for large displays */
@media (min-width: 1600px) {
  .console-width-adaptive {
    max-width: 1800px;
  }
}

/* ============================================================
   TACTICAL NAV BAR (COLLAPSIBLE)
   ============================================================ */

.toc-nav {
  position: sticky;
  top: 0;
  z-index: 50;
  background: linear-gradient(90deg, rgba(5, 7, 14, 0.98), rgba(10, 12, 24, 0.98));
  border-bottom: 1px solid rgba(255, 75, 110, 0.4);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.9);
  transition: transform 0.25s ease, background 0.25s ease, opacity 0.25s ease;
}

.toc-nav-collapsed {
  transform: translateY(-100%);
  opacity: 0.4;
}

.toc-nav-inner {
  max-width: 1800px;
  margin: 0 auto;
  padding: 0.5rem clamp(1.5rem, 4vw, 4rem);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1.5rem;
}

.toc-nav-left {
  display: flex;
  flex-direction: column;
  gap: 0.1rem;
}

.toc-nav-callsign {
  font-family: var(--font-mono);
  font-size: 0.9rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--accent-red);
}

.toc-nav-label {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: var(--text-muted);
}

.toc-nav-links {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
}

.toc-nav-links a {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--accent-cyan);
  text-decoration: none;
  padding: 0.25rem 0.6rem;
  border-radius: 999px;
  border: 1px solid rgba(60, 242, 255, 0.4);
  background: radial-gradient(circle at top left, rgba(60, 242, 255, 0.18), transparent 60%);
}

.toc-nav-links a:hover {
  background: rgba(60, 242, 255, 0.12);
}

/* ============================================================
   NORAD HERO SECTION
   ============================================================ */

.hero-norad {
  margin-top: 1.5rem;
  border-radius: var(--radius-lg);
  background: radial-gradient(circle at top, rgba(255, 75, 110, 0.12), rgba(5, 7, 14, 0.98));
  box-shadow: var(--shadow-hard);
  position: relative;
  overflow: hidden;
}

.hero-grid {
  position: relative;
  display: grid;
  grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr);
  gap: 2.5rem;
  z-index: 2;
}

.hero-left {
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
}

.hero-badge {
  display: inline-flex;
  flex-direction: column;
  padding: 0.5rem 0.9rem;
  border-radius: var(--radius-md);
  border: 1px solid rgba(255, 75, 110, 0.6);
  background: radial-gradient(circle at top left, rgba(255, 75, 110, 0.18), transparent 60%);
  font-family: var(--font-mono);
  font-size: 0.8rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.hero-badge-label {
  color: var(--text-muted);
}

.hero-badge-call {
  color: var(--accent-red);
  font-size: 1rem;
}

.hero-title {
  font-family: var(--font-mono);
  font-size: clamp(1.8rem, 3vw, 2.6rem);
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--accent-cyan);
  text-shadow: 0 0 20px rgba(60, 242, 255, 0.7);
}

.hero-operator {
  font-size: 0.95rem;
  color: var(--text-muted);
}

.hero-highlight {
  color: var(--accent-amber);
  font-family: var(--font-mono);
}

.hero-mission {
  font-size: 0.95rem;
  color: var(--text-main);
  max-width: 34rem;
}

.hero-status-row {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 0.9rem;
  margin-top: 0.5rem;
}

.hero-status-tile {
  padding: 0.7rem 0.8rem;
  border-radius: var(--radius-md);
  background: linear-gradient(145deg, rgba(10, 14, 26, 0.98), rgba(6, 8, 18, 0.98));
  border: 1px solid rgba(255, 75, 110, 0.35);
  box-shadow: 0 0 18px rgba(0, 0, 0, 0.8);
}

.hero-status-label {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--text-muted);
}

.hero-status-value {
  margin-top: 0.2rem;
  font-family: var(--font-mono);
  font-size: 1.05rem;
  color: var(--accent-cyan);
}

.hero-status-meta {
  margin-top: 0.15rem;
  font-size: 0.7rem;
  color: var(--text-muted);
}

.hero-right {
  position: relative;
  display: flex;
  align-items: stretch;
}

.hero-panel {
  position: relative;
  z-index: 2;
  flex: 1;
  border-radius: var(--radius-md);
  background: linear-gradient(145deg, rgba(10, 14, 26, 0.98), rgba(6, 8, 18, 0.98));
  border: 1px solid rgba(255, 75, 110, 0.5);
  box-shadow: 0 0 24px rgba(0, 0, 0, 0.9);
  padding: 1rem 1.1rem 1.2rem;
}

.hero-panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 0.4rem;
}

.hero-panel-title {
  font-family: var(--font-mono);
  font-size: 0.8rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--text-muted);
}

.hero-panel-state {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  padding: 0.15rem 0.6rem;
  border-radius: 999px;
  border: 1px solid rgba(76, 255, 179, 0.7);
}

.hero-panel-state-good {
  color: var(--accent-green);
  background: rgba(0, 180, 120, 0.18);
}

.hero-panel-body {
  font-size: 0.85rem;
  color: var(--text-main);
}

/* HERO RADAR / SCANLINES */

.hero-radar-overlay {
  position: absolute;
  inset: -20%;
  pointer-events: none;
}

.hero-radar-sweep {
  position: absolute;
  width: 220%;
  height: 220%;
  top: -60%;
  left: -60%;
  background: conic-gradient(from 0deg, rgba(255, 75, 110, 0.35), transparent 40%, transparent 100%);
  animation: radar-rotate 18s linear infinite;
  mix-blend-mode: screen;
  opacity: 0.25;
}

.hero-scanlines {
  position: absolute;
  inset: 0;
  background-image: linear-gradient(to bottom, rgba(255, 255, 255, 0.06) 1px, transparent 1px);
  background-size: 100% 3px;
  mix-blend-mode: soft-light;
  opacity: 0.35;
}

@keyframes radar-rotate {
  to {
    transform: rotate(360deg);
  }
}

/* ============================================================
   SECTION HEADERS
   ============================================================ */

.section-header h2 {
  font-family: var(--font-mono);
  font-size: 0.95rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--accent-cyan);
  margin: 0 0 0.4rem;
}

.section-header p {
  margin: 0 0 1.4rem;
  font-size: 0.9rem;
  color: var(--text-muted);
}

/* ============================================================
   AOC SECTION
   ============================================================ */

.section-aoc {
  border-radius: var(--radius-lg);
  background: linear-gradient(145deg, rgba(6, 12, 26, 0.98), rgba(4, 8, 18, 0.98));
  box-shadow: var(--shadow-hard);
}

.intel-panel {
  border-radius: var(--radius-md);
  background: radial-gradient(circle at top, rgba(74, 168, 255, 0.16), rgba(8, 12, 24, 0.98));
  border: 1px solid rgba(74, 168, 255, 0.5);
  padding: 1rem 1.1rem 1.2rem;
}

.intel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 0.75rem;
  margin-bottom: 0.4rem;
}

.intel-title {
  font-family: var(--font-mono);
  font-size: 0.85rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--text-main);
}

.intel-tag {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  padding: 0.15rem 0.6rem;
  border-radius: 999px;
  border: 1px solid rgba(60, 242, 255, 0.6);
}

.intel-tag-aoc {
  color: var(--accent-blue);
  background: rgba(74, 168, 255, 0.16);
}

.intel-tag-jsoc {
  color: var(--accent-amber);
  background: rgba(255, 179, 71, 0.16);
}

.intel-tag-ufo {
  color: var(--accent-violet);
  background: rgba(199, 125, 255, 0.16);
}

.intel-tag-sigint {
  color: var(--accent-green);
  background: rgba(76, 255, 179, 0.16);
}

.intel-tag-eoc {
  color: var(--accent-red);
  background: rgba(255, 75, 110, 0.16);
}

.intel-brief {
  margin-top: 0.8rem;
  font-size: 0.85rem;
  color: var(--text-muted);
}

.intel-brief h3 {
  font-family: var(--font-mono);
  font-size: 0.8rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--accent-blue);
  margin: 0 0 0.3rem;
}

/* ============================================================
   JSOC SECTION
   ============================================================ */

.section-jsoc {
  border-radius: var(--radius-lg);
  background: linear-gradient(145deg, rgba(4, 6, 14, 0.98), rgba(2, 4, 10, 0.98));
  box-shadow: var(--shadow-hard);
}

/* ============================================================
   UFO SECTION
   ============================================================ */

.section-ufo {
  border-radius: var(--radius-lg);
  background: radial-gradient(circle at top, rgba(199, 125, 255, 0.18), rgba(4, 6, 14, 0.98));
  box-shadow: var(--shadow-hard);
}

/* ============================================================
   SIGINT SECTION
   ============================================================ */

.section-sigint {
  border-radius: var(--radius-lg);
  background: linear-gradient(145deg, rgba(4, 8, 16, 0.98), rgba(2, 4, 10, 0.98));
  box-shadow: var(--shadow-hard);
}

/* ============================================================
   EOC / PSY SECTION
   ============================================================ */

.section-eoc-psy {
  border-radius: var(--radius-lg);
  background: linear-gradient(135deg, #1a1030, #0b1b2f, #1f1140);
  box-shadow: 0 0 60px rgba(199, 125, 255, 0.5);
}

/* ============================================================
   IFRAMES
   ============================================================ */

.iframe-stack {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

.iframe-block {
  border-radius: var(--radius-md);
  padding: 1rem 1.1rem 1.2rem;
  background: radial-gradient(circle at top, rgba(60, 242, 255, 0.08), rgba(10, 14, 26, 0.98));
  border: 1px solid rgba(60, 242, 255, 0.18);
  box-shadow: 0 0 24px rgba(0, 0, 0, 0.9);
}

.iframe-frame {
  position: relative;
  border-radius: 14px;
  overflow: hidden;
  margin-top: 0.6rem;
  height: 800px; /* CINEMATIC HEIGHT */
}

.iframe-frame iframe {
  width: 100%;
  height: 100%;
  border: none;
  display: block;
}

/* Frame themes */

.iframe-aoc {
  border: 1px solid rgba(74, 168, 255, 0.7);
  box-shadow: 0 0 40px rgba(74, 168, 255, 0.4);
}

.iframe-jsoc {
  border: 1px solid rgba(255, 179, 71, 0.7);
  box-shadow: 0 0 40px rgba(255, 179, 71, 0.4);
}

.iframe-ufo {
  border: 1px solid rgba(199, 125, 255, 0.7);
  box-shadow: 0 0 45px rgba(199, 125, 255, 0.45);
}

.iframe-sigint {
  border: 1px solid rgba(60, 242, 255, 0.7);
  box-shadow: 0 0 40px rgba(60, 242, 255, 0.4);
}

.iframe-eoc {
  border: 1px solid rgba(255, 75, 110, 0.7);
  box-shadow: 0 0 50px rgba(255, 75, 110, 0.5);
}

/* ============================================================
   BUTTONS / LINKS
   ============================================================ */

.intel